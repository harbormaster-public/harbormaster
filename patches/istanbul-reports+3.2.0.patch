diff --git a/node_modules/istanbul-reports/lib/html/annotator.js b/node_modules/istanbul-reports/lib/html/annotator.js
index 982d047..dc6d9d9 100644
--- a/node_modules/istanbul-reports/lib/html/annotator.js
+++ b/node_modules/istanbul-reports/lib/html/annotator.js
@@ -166,6 +166,9 @@ function annotateBranches(fileCoverage, structuredText) {
             ) {
                 count = branchArray[i];
                 meta = metaArray[i];
+                if (!meta || !meta.start || !meta.end) {
+                    continue;
+                }
                 startCol = meta.start.column;
                 endCol = meta.end.column + 1;
                 startLine = meta.start.line;
@@ -191,10 +194,15 @@ function annotateBranches(fileCoverage, structuredText) {
                     branchMeta[branchName].type === 'if'
                 ) {
                     const prevMeta = metaArray[i - 1];
-                    startCol = prevMeta.start.column;
-                    endCol = prevMeta.end.column + 1;
-                    startLine = prevMeta.start.line;
-                    endLine = prevMeta.end.line;
+                    if (prevMeta && prevMeta.start && prevMeta.end) {
+                        startCol = prevMeta.start.column;
+                        endCol = prevMeta.end.column + 1;
+                        startLine = prevMeta.start.line;
+                        endLine = prevMeta.end.line;
+                    } else {
+                        // Skip this branch if previous meta is invalid
+                        continue;
+                    }
                 }
 
                 if (count === 0 && structuredText[startLine]) {
